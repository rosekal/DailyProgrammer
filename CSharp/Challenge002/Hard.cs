using System;
using System.Threading;

namespace Challenge002 {
    class Hard{
        /*
        static System.Timers.Timer sw = new Timers.Timer();
        public static void Run() {
            while (true) {
                Console.WriteLine("Welcome to the stopwatch application!\n\nPress 1 to start!");
                string input = Console.ReadLine();


                sw. += new EventHandler(Runnable);

                Thread t = new Thread(() => Runnable());
                t.Start();
            }
        }

        public static void Runnable() {
            sw.Start();

            while (true) {
                int seconds = (int) sw.ElapsedMilliseconds / 1000;
                
                if(sw.ElapsedMilliseconds % 1000 == 0)
                    Console.WriteLine(seconds);
            }
        }*/
    }
}
